fix: cross-platform support for Windows, macOS, and Linux

- server.ts: add Unix port-kill fallback (lsof on macOS/Linux, fuser on
  Linux) so `xmv stop` works without PowerShell. Manual kill hint now
  adapts to platform (taskkill / lsof / fuser).
- doctor.ts: copy command adapts to platform (copy on Windows cmd, cp on
  Unix). nextSteps no longer hardcode Windows-only commands.
- help.ts: log file path now shows all three platform locations
  (Windows %LOCALAPPDATA%, macOS ~/Library/Logs, Linux ~/.local/share).
- feedback.ts, file-logger.ts, logger.ts: update comments to reflect
  platform-specific log locations instead of Windows-only AppData.
- README.md: add cross-platform section with per-OS requirements
  (Build Tools on Windows, Xcode CLT on macOS, build-essential on Linux),
  platform-specific copy commands, CLI usage, and log file location table.

Logger paths were already cross-platform (Win AppData / macOS Library/Logs
/ Linux XDG_DATA_HOME). This commit fixes the remaining Windows-only
assumptions in stop logic, doctor hints, help text, and docs.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
